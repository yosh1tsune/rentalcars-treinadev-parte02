<h1>Modelos de Carros</h1>
<%= link_to 'Registrar novo modelo', new_car_model_path, class: 'btn btn-primary mb-1 mt-1' %>
<% if @car_models.any? %>
  <ul>
    <% @car_models.each do |model| %>
      <li class='mb-1 mt-1'><%= link_to model.name, model %></li>
    <% end %>
  </ul>
<% end %>
