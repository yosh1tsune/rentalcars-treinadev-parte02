<h1>Filiais</h1>
<%= link_to 'Registrar nova filial', new_subsidiary_path, class: 'btn btn-primary mt-1 mb-1' %>
<% if @subsidiaries.any? %>
<ul>
  <% @subsidiaries.each do |subsidiary| %>
    <li class='mb-1 mt-1'><%= link_to subsidiary.name, subsidiary %></li>
  <% end %>
</ul>
<% end %>
